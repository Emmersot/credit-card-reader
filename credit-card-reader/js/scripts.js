// 4102 0808 8043 5620
let modArray = ["4","1","0","2","0","8","0","8","8","0","4","3","5","6","2","0"]

  function toDouble(modArray) 
  {
  let modified = new Array();
  for (let i = modArray.length - 1; i = 0; i += 2) 
  {
    if (i * 2 == 0) 
    {
      modified.push(modArray[index]);
      console.log("0 is working")
    }
    // else if 
    // {
    //   modified.push(modArray[index] * 2);
    //   console.log("x2 is working")
    // }
    // else (i >=10 ) 
    // {
    //   modified.push(modArray[index] % 2, + ia + ib);
    // }
  }
  return modified;
  }



"This card number is not valid."
"This card number is valid."